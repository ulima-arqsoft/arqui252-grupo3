> [9. Metodología de Diseño de Arquitectura - Aplicación de ADD](../../9.md) › [9.4. Iteración 3: Refinar estructuras para abordar el atributo de calidad más importante](../9.4.md) › [9.4.1. Objetivo de la iteración](9.4.1.md)

# 9.4.1. Objetivo de la iteración

El objetivo principal de esta tercera iteración es refinar las estructuras arquitectónicas para garantizar el cumplimiento del atributo de calidad más crítico identificado en el sistema InnovaLogix Retail ERP: la **disponibilidad y consistencia en tiempo real** de la información de inventario entre múltiples usuarios y módulos.

Esta iteración se enfoca específicamente en consolidar las decisiones arquitectónicas relacionadas con la **sincronización en tiempo real mediante WebSocket**, el **manejo de eventos de dominio**, y las **estrategias de cache y persistencia** que permitan mantener la coherencia de datos incluso bajo condiciones de alta concurrencia o fallos de red temporal.

Los elementos específicos a refinar incluyen:

Primero, el **sistema de notificaciones en tiempo real** basado en Socket.IO que propaga cambios de stock, nuevas ventas y actualizaciones de productos a todos los clientes conectados con latencias inferiores a 100 milisegundos. Este sistema debe garantizar que cuando un usuario modifica el inventario desde el módulo de Almacén, todos los cajeros en el módulo POS vean inmediatamente la actualización sin necesidad de refrescar manualmente sus pantallas.

Segundo, la **arquitectura Event-Driven** que desacopla los módulos mediante eventos de dominio como `stockUpdate`, `productCreated`, `saleCompleted` y `lowStockAlert`. Esta estructura permite que módulos independientes reaccionen a cambios sin acoplamiento directo, facilitando la escalabilidad y mantenibilidad del sistema.

Tercero, el **patrón Cache-Aside con invalidación automática** implementado mediante node-cache, que reduce la carga en PostgreSQL manteniendo datos frecuentemente consultados en memoria con tiempos de respuesta inferiores a 50 milisegundos, mientras garantiza la consistencia mediante invalidación sincronizada con escrituras en base de datos.

Cuarto, las **transacciones ACID con PostgreSQL** que aseguran la integridad de operaciones críticas como ajustes de inventario, registro de ventas y actualización de stock, previniendo estados inconsistentes incluso ante fallos de conexión durante la transacción.

Finalmente, el **soporte offline mediante PWA** con Service Workers e IndexedDB que permite la continuidad operativa del módulo POS durante caídas temporales de internet, sincronizando automáticamente las operaciones pendientes al restablecer la conexión.

Al finalizar esta iteración, el sistema debe demostrar capacidad para mantener sincronizados hasta 50 usuarios concurrentes consultando y modificando inventario, con tiempos de propagación de eventos inferiores a 200 milisegundos en el percentil 95, tasa de entrega de notificaciones superior al 99.5%, y cero pérdida de datos en operaciones críticas incluso ante desconexiones temporales de hasta 30 minutos.

---

[🏠 Home](../../../README.md) | [Siguiente ➡️](../9.4.2/9.4.2.md)